apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: result-dev-se-eva
  annotations:
    codefresh.io/environment: development
    codefresh.io/product: se-result
spec:
  project: default
  source:
    repoURL: https://github.com/salesdemocf/cf-configs.git
    targetRevision: se
    path: environments/development/se/cf-result
  destination:
    namespace: eva-se-dev
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      allowEmpty: true
      prune: true
      selfHeal: true
    syncOptions:
      - Validate=false
      - CreateNamespace=true
