apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: result-env-s-abbr-eva
  annotations:
    codefresh.io/product: abbr-result
spec:
  project: default
  source:
    repoURL: https://github.com/organization/configs.git
    targetRevision: HEAD
    path: environments/env-l/abbr/result
  destination:
    namespace: eva-abbr-env-s
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      allowEmpty: true
      prune: true
      selfHeal: true
    syncOptions:
      - Validate=false
      - CreateNamespace=true
