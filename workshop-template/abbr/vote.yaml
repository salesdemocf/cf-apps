apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vote-env-s-abbr-eva
  annotations:
    codefresh.io/product: abbr-vote
spec:
  project: default
  source:
    repoURL: https://github.com/organization/configs.git
    targetRevision: HEAD
    path: environments/env-l/abbr/vote
  destination:
    namespace: eva-abbr-env-s
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      allowEmpty: true
      prune: true
      selfHeal: true
    syncOptions:
      - Validate=false
      - CreateNamespace=true
